  CREATE TABLE "ACCOUNT" 
   (	
        "SSN" NUMBER NOT NULL ENABLE, 
	"F_NAME" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"L_NAME" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"B_DATE" DATE, 
	"NATIONALITY" VARCHAR2(20 BYTE), 
	"PASSWORD" VARCHAR2(20 BYTE), 
	"PHONE" VARCHAR2(20 BYTE), 
	"GENDER" VARCHAR2(20 BYTE), 
	 CONSTRAINT "ACCOUNT_PK" PRIMARY KEY ("SSN") 
   );

  CREATE TABLE "ADMIN" 
   (	
        "SSN" NUMBER NOT NULL ENABLE, 
	"SALARY" NUMBER, 
	 CONSTRAINT "ADMIN_PK" PRIMARY KEY ("SSN")
   );

  CREATE TABLE "AIRLINE" 
   (	"AIRLINEID" NUMBER NOT NULL ENABLE, 
	"AIRLINENAME" VARCHAR2(20 BYTE), 
	"COUNTRY" VARCHAR2(20 BYTE)
  );

 CREATE TABLE "CUSTOMER" 
   (	"SSN" NUMBER NOT NULL ENABLE, 
	"CREDIT_CARD_NUM" NUMBER, 
	"REGISTERATION_DATE" DATE, 
	"PASSPORT_NUM" VARCHAR2(20 BYTE), 
	 CONSTRAINT "CUSTOMER_PK" PRIMARY KEY ("SSN") );


  CREATE TABLE "FLIGHT" 
   (	"FLIGHT_ID" NUMBER NOT NULL ENABLE, 
	"ARRIVAL_TIME" NUMBER, 
	"ARRIVAL_AIRPORT" VARCHAR2(20 BYTE), 
	"DATE" DATE, 
	"NUMBER_OF_SEATS" NUMBER, 
	"DEPARTURE_TIME" NUMBER, 
	"DEPARTURE_AIRPORT" VARCHAR2(20 BYTE), 
	"ADMIN_SSN" NUMBER, 
	"AIRLINEID" NUMBER, 
	 CONSTRAINT "FLIGHT_PK" PRIMARY KEY ("FLIGHT_ID") );
 

  CREATE TABLE "RESERVATION" 
   (	"RESERVATION_ID" NUMBER NOT NULL ENABLE, 
	"RESERVATION_DATE" DATE, 
	"CUSTOMER_ID" NUMBER, 
	"FLIGHTID" NUMBER, 
	 CONSTRAINT "RESERVATION_PK" PRIMARY KEY ("RESERVATION_ID"));
 


  CREATE TABLE "SEAT" 
   (	"SEAT_NUM" NUMBER NOT NULL ENABLE, 
	"AVAILABILITY" VARCHAR2(20 BYTE), 
	"RESERVATION_ID" NUMBER, 
	"FLIGHTID" NUMBER, 
	"COST" NUMBER, 
	 CONSTRAINT "SEAT_PK" PRIMARY KEY ("SEAT_NUM"));


  CREATE TABLE "WORKER" 
   (	"SSN" NUMBER NOT NULL ENABLE, 
	"TYPE" VARCHAR2(20 BYTE), 
	"SALARY" VARCHAR2(20 BYTE), 
	"S-DATE" DATE, 
	"AIRPLAINID" NUMBER, 
	 CONSTRAINT "WORKER_PK" PRIMARY KEY ("SSN") );
 

  CREATE TABLE "WORKER_FLIGHT" 
   (	"SSN" NUMBER, 
	"FLIGHTID" NUMBER, 
	 CONSTRAINT "WORKET_FLIGHT_WORKER_FK1" FOREIGN KEY ("SSN")
	  REFERENCES "WORKER" ("SSN") ENABLE, 
	 CONSTRAINT "WORKET_FLIGHT_FLIGHT_FK1" FOREIGN KEY ("FLIGHTID")
	  REFERENCES "FLIGHT" ("FLIGHT_ID") ENABLE);
 


 

